GROQ_API_KEY=
MODELO_LLM=meta-llama/llama-4-scout-17b-16e-instruct

# Datos de negocio (pedidos, incidencias). En docker-compose apunta al Postgres del propio compose.
DATABASE_URL=postgresql+psycopg://triaje:triaje@db:5432/triaje

# Cadena "pelada" (sin +psycopg) para el checkpointer de LangGraph. Vacia = usa SQLite local.
POSTGRES_DSN=postgresql://triaje:triaje@db:5432/triaje

REVIEW_USERNAME=admin
REVIEW_PASSWORD=cambia-esto
SECRET_KEY=cambia-esto-tambien
